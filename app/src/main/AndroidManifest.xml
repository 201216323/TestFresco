<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bruce.chang.testfresco">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FrescoSpimgActivity" />
        <activity android:name=".FrescoCropActivity" />
        <activity android:name=".FrescoCircleActivity" />
        <activity android:name=".FrescoJianJinShiActivity" />
        <activity android:name=".FrescoGifActivity" />
        <activity android:name=".FrescoMultiActivity" />
        <activity android:name=".FrescoListenerActivity" />
        <activity android:name=".FrescoScaleAndRotateActivity" />
        <activity android:name=".FrescoutoSizeImgActivity"></activity>
    </application>

</manifest>